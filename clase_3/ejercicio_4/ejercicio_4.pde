void setup(){
  size(400, 400);
  background(0);
  
  //for loop
  // inicualizamos variable 
  for (int i = 0;  i < width;     i = i + 10){
    for (int j = 0; j < height; j=j+10){
      ellipse(i, j, 50, 50);
      
         // posX  posY
    }
  }
}
